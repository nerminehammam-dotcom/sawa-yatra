import CaravansPage from "@/app/(public)/caravans/page";
import { createPageMetadata } from "@/app/_metadata";

export const metadata = createPageMetadata("/journeys/caravans");

export default CaravansPage;
