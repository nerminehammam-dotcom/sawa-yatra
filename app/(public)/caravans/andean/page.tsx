import { createPageMetadata } from "@/app/_metadata";

import DeparturesPage from "../../departures/page";

export const metadata = createPageMetadata("/caravans/andean");

export default DeparturesPage;
